# Individual contribution report

## Thông tin

- **Họ và tên:** Đậu Quang Ý
- **Vai trò:** Thành viên 1 (Data Engineer / Data Collection & Preprocessing Specialist)
- **Mã học viên:** 2A202602661
- **Nhóm:** K4-L3B-Day08-KhongBiet
- **Repository/branch:** main

## Phần việc đã thực hiện

| Module/deliverable                | Việc tôi trực tiếp làm                                                                                                                                     | File/commit/PR                                           | Trạng thái |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ---------- |
| Task 1: Thu thập văn bản quy chế  | Tìm kiếm nguồn chính thức, viết mã tải tự động 4 tài liệu PDF quy chế đào tạo, điểm rèn luyện, chuẩn ngoại ngữ và an toàn thông tin (> 280KB/file)         | `src/task1_collect_legal_docs.py`, `data/landing/legal/` | Done       |
| Task 2: Crawl tin tức & thông báo | Triển khai crawler BeautifulSoup trích xuất 5 bài viết thông báo học bổng, học phí kèm metadata chuẩn (`url`, `title`, `date_crawled`, `content_markdown`) | `src/task2_crawl_news.py`, `data/landing/news/`          | Done       |
| Task 3: Chuẩn hóa Markdown        | Triển khai pipeline chuẩn hóa sử dụng MarkItDown để chuyển đổi toàn bộ PDF và JSON sang Markdown có cấu trúc, chuẩn bị dữ liệu đầu vào cho nhóm            | `src/task3_convert_markdown.py`, `data/standardized/`    | Done       |

## Cấu trúc dữ liệu và Quy trình làm sạch

- **Cấu trúc lưu trữ:** Phân tách rõ ràng giữa `data/landing/` (dữ liệu thô: 4 PDF quy chế chính thức và 5 JSON tin tức) và `data/standardized/` (Markdown chuẩn hóa: 4 file legal và 5 file news). Các bài viết đều được gắn Header Metadata chuẩn (`# Title`, `**Source:**`, `**Crawled:**`).
- **Nội dung ngữ liệu:** Thu thập trọn vẹn bộ dữ liệu chính quy về sinh viên ĐHQG-HCM gồm: 4 văn bản quy chế (đào tạo tín chỉ, khung điểm rèn luyện, chuẩn ngoại ngữ/IELTS, an toàn thông tin) và 5 bài viết thông báo (học bổng khuyến khích mới, học bổng UIT Global, chính sách miễn giảm học phí).
- **Tại sao cần làm sạch:**
  1. _Chống nhiễu cho Hybrid Search:_ Loại bỏ thẻ menu `<nav>`, footer, script để không làm loãng trọng số từ khóa TF-IDF của BM25 và không làm lệch vector ngữ nghĩa của Dense Retrieval.
  2. _Bảo toàn cấu trúc cho Chunking (Task 4):_ Giữ nguyên các cấp tiêu đề `# Chương`, `## Điều` và bảng biểu `|---|` để bộ Text Splitter cắt theo đúng phạm vi điều khoản, không bị đứt đoạn câu chữ.
  3. _Tối ưu Token & Trích dẫn:_ Giảm 70-80% mã HTML rác, đồng thời cung cấp URL nguồn và ngày crawl để chatbot thực hiện trích dẫn (citation) chính xác chống ảo giác ở Task 10.
- **Cách làm sạch:** Bóc tách vùng nội dung chính bằng BeautifulSoup (`div.field-name-body`), lọc bỏ thẻ rác qua `markdownify(..., strip=['script', 'style', 'nav', 'footer'])`, chuyển đổi bảng biểu PDF bằng `MarkItDown` (`pdfminer-six` + `pdfplumber`) và ép mã hóa `UTF-8`.
  _(Tài liệu kỹ thuật chi tiết xem tại `docs/DATA_CLEANING_REPORT.md` hoặc file PDF `reports/BAO_CAO_DU_LIEU_VA_DEMO_TASK1_3.pdf`)_.

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Sử dụng tài liệu PDF số hóa có text layer trực tiếp thay vì các bản scan dạng hình ảnh.  
   **Lý do/evidence:** Thử nghiệm ban đầu với các file scan ảnh khiến công cụ trích xuất văn bản chỉ thu được vài ký tự, không đủ điều kiện tối thiểu 200 ký tự của bài test chấp thuận. Sau khi chuyển sang PDF số hóa gốc, mỗi file trích xuất đạt từ 28.000 đến 65.000 ký tự văn bản rõ ràng.  
   **Trade-off:** Mất thời gian chọn lọc và kiểm tra kỹ lưỡng định dạng file từ cổng thông tin đào tạo của trường, nhưng đảm bảo chất lượng ngữ liệu tối đa cho các công đoạn chunking và vector search của nhóm.

2. **Quyết định:** Bổ sung header metadata (`Title`, `Source URL`, `Date Crawled`) vào đầu mỗi bài viết Markdown chuẩn hóa.  
   **Lý do/evidence:** Đảm bảo khi chatbot RAG sinh câu trả lời ở Task 10 có thể trích dẫn nguồn (citation) chính xác về URL gốc của thông báo.  
   **Trade-off:** Cần xử lý làm sạch văn bản HTML để tránh các thẻ điều hướng thừa trước khi ghép header.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_acceptance.py -k "test_corpus or test_standardized" -v`
- Kết quả trước/sau:
  - Trước: Chưa có dữ liệu và chưa triển khai hàm, acceptance test bị thiếu dữ liệu.
  - Sau: Đạt 3/3 bài test chấp thuận (`test_corpus_has_required_legal_documents`, `test_corpus_has_required_news_with_metadata`, `test_standardized_output_covers_both_source_types`) với thời gian chạy chỉ 0.3s.
- Lỗi đã phát hiện và cách xử lý: Phát hiện thư viện `markitdown` thiếu module xử lý PDF chuyên dụng (`pdfminer-six`, `pdfplumber`), đã cấu hình cài đặt bổ sung `markitdown[pdf]` để trích xuất mượt mà.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Tốc độ tải và crawl hiện tại phụ thuộc vào phản hồi của máy chủ trường đại học.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung cơ chế retry tự động và cache cục bộ để tối ưu hiệu năng khi crawl số lượng lớn bài viết.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Đậu Quang Ý
